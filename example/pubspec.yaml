name: foil_demo
description: A demonstration of wrapping a widget with Foil.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  xl: any
  # spectrum: 0.2.1+1

dev_dependencies:
  foil:
    path: ../

flutter:
  uses-material-design: true
